import AppLayout from '@/Layouts/AppLayout';
import Card from '@/Components/Card';
import Input from '@/Components/Input';
import Button from '@/Components/Button';
import { Head, useForm } from '@inertiajs/react';
import { IconDeviceFloppy, IconChartBarPopular } from '@tabler/icons-react';

const defaultData = { analytics_no: '', report_title: '', report_period: '', metric_category: 'reliability', insight_summary: '', anomaly_count: 0, prediction_accuracy: 0, recommended_action: '', prepared_by: '', status: 'draft' };

export default function Create({ users = [], initialData = defaultData, isEdit = false }) {
    const { data, setData, post, put, errors } = useForm({ ...defaultData, ...initialData, prepared_by: initialData.prepared_by ?? '' });
    const submit = (e) => { e.preventDefault(); isEdit ? put(route('apps.advanced-reporting-analytics.update', initialData.id)) : post(route('apps.advanced-reporting-analytics.store')); };

    return <><Head title={isEdit ? 'Ubah Advanced Reporting & Analytics' : 'Tambah Advanced Reporting & Analytics'} /><Card title={isEdit ? 'Ubah Advanced Reporting & Analytics' : 'Tambah Advanced Reporting & Analytics'} icon={<IconChartBarPopular size={18} strokeWidth={1.5} />} form={submit} footer={<Button type="submit" icon={<IconDeviceFloppy size={18} strokeWidth={1.5} />} label="Simpan" variant="gray" />}><div className="grid md:grid-cols-2 gap-4 mb-4"><Input label="No Analytics" type="text" value={data.analytics_no} onChange={(e) => setData('analytics_no', e.target.value)} errors={errors.analytics_no} /><Input label="Judul Report" type="text" value={data.report_title} onChange={(e) => setData('report_title', e.target.value)} errors={errors.report_title} /><Input label="Periode Report" type="date" value={data.report_period} onChange={(e) => setData('report_period', e.target.value)} errors={errors.report_period} /><div><label className="text-gray-600 text-sm">Kategori Metric</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.metric_category} onChange={(e) => setData('metric_category', e.target.value)}><option value="reliability">Reliability</option><option value="cost">Cost</option><option value="energy">Energy</option><option value="sparepart">Sparepart</option><option value="executive">Executive</option></select></div><Input label="Jumlah Anomali" type="number" value={data.anomaly_count} onChange={(e) => setData('anomaly_count', e.target.value)} errors={errors.anomaly_count} /><Input label="Prediction Accuracy (%)" type="number" value={data.prediction_accuracy} onChange={(e) => setData('prediction_accuracy', e.target.value)} errors={errors.prediction_accuracy} /><div><label className="text-gray-600 text-sm">Prepared By</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.prepared_by} onChange={(e) => setData('prepared_by', e.target.value)}><option value="">- Pilih User -</option>{users.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><div><label className="text-gray-600 text-sm">Status</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.status} onChange={(e) => setData('status', e.target.value)}><option value="draft">Draft</option><option value="published">Published</option><option value="archived">Archived</option></select></div><div className="md:col-span-2"><label className="text-gray-600 text-sm">Insight Summary</label><textarea className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" rows={4} value={data.insight_summary} onChange={(e) => setData('insight_summary', e.target.value)} /></div><div className="md:col-span-2"><label className="text-gray-600 text-sm">Recommended Action</label><textarea className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" rows={4} value={data.recommended_action ?? ''} onChange={(e) => setData('recommended_action', e.target.value)} /></div></div></Card></>;
}

Create.layout = (page) => <AppLayout children={page} />;
