import AppLayout from '@/Layouts/AppLayout';
import Card from '@/Components/Card';
import Input from '@/Components/Input';
import Button from '@/Components/Button';
import { Head, useForm } from '@inertiajs/react';
import { IconDeviceFloppy, IconTool } from '@tabler/icons-react';

const defaultData = { flow_no: '', work_order_id: '', technician_id: '', checkin_at: '', checkout_at: '', action_taken: '', sparepart_used: '', verification_note: '', status: 'assigned' };

export default function Create({ workOrders = [], users = [], initialData = defaultData, isEdit = false }) {
    const { data, setData, post, put, errors } = useForm({ ...defaultData, ...initialData, work_order_id: initialData.work_order_id ?? '', technician_id: initialData.technician_id ?? '' });
    const submit = (e) => { e.preventDefault(); isEdit ? put(route('apps.mobile-technician-flows.update', initialData.id)) : post(route('apps.mobile-technician-flows.store')); };

    return <><Head title={isEdit ? 'Ubah Mobile Technician Flow' : 'Tambah Mobile Technician Flow'} /><Card title={isEdit ? 'Ubah Mobile Technician Flow' : 'Tambah Mobile Technician Flow'} icon={<IconTool size={18} strokeWidth={1.5} />} form={submit} footer={<Button type="submit" icon={<IconDeviceFloppy size={18} strokeWidth={1.5} />} label="Simpan" variant="gray" />}><div className="grid md:grid-cols-2 gap-4 mb-4"><Input label="Flow No" type="text" value={data.flow_no} onChange={(e) => setData('flow_no', e.target.value)} errors={errors.flow_no} /><div><label className="text-gray-600 text-sm">Work Order</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.work_order_id} onChange={(e) => setData('work_order_id', e.target.value)}><option value="">- Pilih WO -</option>{workOrders.map((item) => <option key={item.id} value={item.id}>{item.wo_no}</option>)}</select></div><div><label className="text-gray-600 text-sm">Teknisi</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.technician_id} onChange={(e) => setData('technician_id', e.target.value)}><option value="">- Pilih Teknisi -</option>{users.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><Input label="Check-in" type="datetime-local" value={data.checkin_at ?? ''} onChange={(e) => setData('checkin_at', e.target.value)} errors={errors.checkin_at} /><Input label="Check-out" type="datetime-local" value={data.checkout_at ?? ''} onChange={(e) => setData('checkout_at', e.target.value)} errors={errors.checkout_at} /><div><label className="text-gray-600 text-sm">Status</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.status} onChange={(e) => setData('status', e.target.value)}><option value="assigned">Assigned</option><option value="on_the_way">On The Way</option><option value="on_site">On Site</option><option value="working">Working</option><option value="completed">Completed</option><option value="verified">Verified</option></select></div></div><div className="grid gap-4"><div><label className="text-gray-600 text-sm">Action Taken</label><textarea className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" rows={3} value={data.action_taken} onChange={(e) => setData('action_taken', e.target.value)} /></div><div><label className="text-gray-600 text-sm">Sparepart Used</label><textarea className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" rows={3} value={data.sparepart_used} onChange={(e) => setData('sparepart_used', e.target.value)} /></div><div><label className="text-gray-600 text-sm">Verification Note</label><textarea className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" rows={3} value={data.verification_note} onChange={(e) => setData('verification_note', e.target.value)} /></div></div></Card></>;
}

Create.layout = (page) => <AppLayout children={page} />;
