import AppLayout from '@/Layouts/AppLayout';
import Card from '@/Components/Card';
import Input from '@/Components/Input';
import Button from '@/Components/Button';
import { Head, useForm } from '@inertiajs/react';
import { IconCalendarDue, IconDeviceFloppy } from '@tabler/icons-react';

const defaultData = { plan_no: '', title: '', plan_date: '', start_time: '', end_time: '', asset_id: '', work_order_id: '', assigned_to: '', plan_type: 'pm', priority: 'medium', status: 'scheduled', note: '' };

export default function Create({ assets = [], workOrders = [], users = [], initialData = defaultData, isEdit = false }) {
    const { data, setData, post, put, errors } = useForm({ ...defaultData, ...initialData, asset_id: initialData.asset_id ?? '', work_order_id: initialData.work_order_id ?? '', assigned_to: initialData.assigned_to ?? '' });
    const submit = (e) => { e.preventDefault(); isEdit ? put(route('apps.planner-calendars.update', initialData.id)) : post(route('apps.planner-calendars.store')); };

    return <><Head title={isEdit ? 'Ubah Planner Calendar' : 'Tambah Planner Calendar'} /><Card title={isEdit ? 'Ubah Planner Calendar' : 'Tambah Planner Calendar'} icon={<IconCalendarDue size={18} strokeWidth={1.5} />} form={submit} footer={<Button type="submit" icon={<IconDeviceFloppy size={18} strokeWidth={1.5} />} label="Simpan" variant="gray" />}><div className="grid md:grid-cols-2 gap-4 mb-4"><Input label="No Plan" type="text" value={data.plan_no} onChange={(e) => setData('plan_no', e.target.value)} errors={errors.plan_no} /><Input label="Tanggal" type="date" value={data.plan_date} onChange={(e) => setData('plan_date', e.target.value)} errors={errors.plan_date} /><Input label="Judul" type="text" value={data.title} onChange={(e) => setData('title', e.target.value)} errors={errors.title} /><Input label="Jam Mulai" type="time" value={data.start_time ?? ''} onChange={(e) => setData('start_time', e.target.value)} errors={errors.start_time} /><Input label="Jam Selesai" type="time" value={data.end_time ?? ''} onChange={(e) => setData('end_time', e.target.value)} errors={errors.end_time} /><div><label className="text-gray-600 text-sm">Asset</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.asset_id} onChange={(e) => setData('asset_id', e.target.value)}><option value="">- Pilih Asset -</option>{assets.map((item) => <option key={item.id} value={item.id}>{item.asset_name}</option>)}</select></div><div><label className="text-gray-600 text-sm">Work Order</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.work_order_id} onChange={(e) => setData('work_order_id', e.target.value)}><option value="">- Pilih WO -</option>{workOrders.map((item) => <option key={item.id} value={item.id}>{item.wo_no}</option>)}</select></div><div><label className="text-gray-600 text-sm">Assigned To</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.assigned_to} onChange={(e) => setData('assigned_to', e.target.value)}><option value="">- Pilih User -</option>{users.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><div><label className="text-gray-600 text-sm">Plan Type</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.plan_type} onChange={(e) => setData('plan_type', e.target.value)}><option value="pm">PM</option><option value="cm">CM</option><option value="inspection">Inspection</option><option value="calibration">Calibration</option><option value="project">Project</option></select></div><div><label className="text-gray-600 text-sm">Priority</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.priority} onChange={(e) => setData('priority', e.target.value)}><option value="low">Low</option><option value="medium">Medium</option><option value="high">High</option><option value="emergency">Emergency</option></select></div><div><label className="text-gray-600 text-sm">Status</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.status} onChange={(e) => setData('status', e.target.value)}><option value="scheduled">Scheduled</option><option value="in_progress">In Progress</option><option value="done">Done</option><option value="cancelled">Cancelled</option></select></div></div><div><label className="text-gray-600 text-sm">Catatan</label><textarea className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" rows={4} value={data.note} onChange={(e) => setData('note', e.target.value)} /></div></Card></>;
}

Create.layout = (page) => <AppLayout children={page} />;
