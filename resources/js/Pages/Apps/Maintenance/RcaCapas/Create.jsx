import AppLayout from '@/Layouts/AppLayout';
import Card from '@/Components/Card';
import Input from '@/Components/Input';
import Button from '@/Components/Button';
import { Head, useForm } from '@inertiajs/react';
import { IconActivityHeartbeat, IconDeviceFloppy } from '@tabler/icons-react';

const defaultData = { rca_no: '', work_order_id: '', problem_statement: '', root_cause: '', corrective_action: '', preventive_action: '', owner_id: '', due_date: '', status: 'open' };

export default function Create({ workOrders = [], users = [], initialData = defaultData, isEdit = false }) {
    const { data, setData, post, put, errors } = useForm({ ...defaultData, ...initialData, work_order_id: initialData.work_order_id ?? '', owner_id: initialData.owner_id ?? '' });
    const submit = (e) => { e.preventDefault(); isEdit ? put(route('apps.rca-capas.update', initialData.id)) : post(route('apps.rca-capas.store')); };

    return <><Head title={isEdit ? 'Ubah RCA/CAPA' : 'Tambah RCA/CAPA'} /><Card title={isEdit ? 'Ubah RCA/CAPA' : 'Tambah RCA/CAPA'} icon={<IconActivityHeartbeat size={18} strokeWidth={1.5} />} form={submit} footer={<Button type="submit" icon={<IconDeviceFloppy size={18} strokeWidth={1.5} />} label="Simpan" variant="gray" />}><div className="grid md:grid-cols-2 gap-4 mb-4"><Input label="No RCA" type="text" value={data.rca_no} onChange={(e) => setData('rca_no', e.target.value)} errors={errors.rca_no} /><Input label="Due Date" type="date" value={data.due_date ?? ''} onChange={(e) => setData('due_date', e.target.value)} errors={errors.due_date} /><div><label className="text-gray-600 text-sm">Work Order</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.work_order_id} onChange={(e) => setData('work_order_id', e.target.value)}><option value="">- Pilih WO -</option>{workOrders.map((item) => <option key={item.id} value={item.id}>{item.wo_no}</option>)}</select></div><div><label className="text-gray-600 text-sm">Owner</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.owner_id} onChange={(e) => setData('owner_id', e.target.value)}><option value="">- Pilih User -</option>{users.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><div><label className="text-gray-600 text-sm">Status</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.status} onChange={(e) => setData('status', e.target.value)}><option value="open">Open</option><option value="in_progress">In Progress</option><option value="verified">Verified</option><option value="closed">Closed</option></select></div></div><div className="grid gap-4"><div><label className="text-gray-600 text-sm">Problem Statement</label><textarea className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" rows={3} value={data.problem_statement} onChange={(e) => setData('problem_statement', e.target.value)} /></div><div><label className="text-gray-600 text-sm">Root Cause</label><textarea className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" rows={3} value={data.root_cause} onChange={(e) => setData('root_cause', e.target.value)} /></div><div><label className="text-gray-600 text-sm">Corrective Action</label><textarea className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" rows={3} value={data.corrective_action} onChange={(e) => setData('corrective_action', e.target.value)} /></div><div><label className="text-gray-600 text-sm">Preventive Action</label><textarea className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" rows={3} value={data.preventive_action} onChange={(e) => setData('preventive_action', e.target.value)} /></div></div></Card></>;
}

Create.layout = (page) => <AppLayout children={page} />;
