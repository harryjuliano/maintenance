import AppLayout from '@/Layouts/AppLayout';
import Card from '@/Components/Card';
import Input from '@/Components/Input';
import Button from '@/Components/Button';
import { Head, useForm } from '@inertiajs/react';
import { IconDeviceFloppy, IconPlugConnected } from '@tabler/icons-react';

const defaultData = { integration_no: '', system_name: '', integration_type: 'api', endpoint: '', sync_frequency: 'daily', last_sync_at: '', success_rate_pct: 0, owner_id: '', status: 'planned', note: '' };

export default function Create({ users = [], initialData = defaultData, isEdit = false }) {
    const { data, setData, post, put, errors } = useForm({ ...defaultData, ...initialData, owner_id: initialData.owner_id ?? '' });
    const submit = (e) => { e.preventDefault(); isEdit ? put(route('apps.cross-system-integrations.update', initialData.id)) : post(route('apps.cross-system-integrations.store')); };

    return <><Head title={isEdit ? 'Ubah Integrasi Lintas Sistem' : 'Tambah Integrasi Lintas Sistem'} /><Card title={isEdit ? 'Ubah Integrasi Lintas Sistem' : 'Tambah Integrasi Lintas Sistem'} icon={<IconPlugConnected size={18} strokeWidth={1.5} />} form={submit} footer={<Button type="submit" icon={<IconDeviceFloppy size={18} strokeWidth={1.5} />} label="Simpan" variant="gray" />}><div className="grid md:grid-cols-2 gap-4 mb-4"><Input label="No Integrasi" type="text" value={data.integration_no} onChange={(e) => setData('integration_no', e.target.value)} errors={errors.integration_no} /><Input label="Nama Sistem" type="text" value={data.system_name} onChange={(e) => setData('system_name', e.target.value)} errors={errors.system_name} /><div><label className="text-gray-600 text-sm">Tipe Integrasi</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.integration_type} onChange={(e) => setData('integration_type', e.target.value)}><option value="api">API</option><option value="etl">ETL</option><option value="iot">IoT</option><option value="manual">Manual</option></select></div><Input label="Endpoint" type="text" value={data.endpoint ?? ''} onChange={(e) => setData('endpoint', e.target.value)} errors={errors.endpoint} /><div><label className="text-gray-600 text-sm">Frekuensi Sinkronisasi</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.sync_frequency} onChange={(e) => setData('sync_frequency', e.target.value)}><option value="realtime">Realtime</option><option value="hourly">Hourly</option><option value="daily">Daily</option><option value="weekly">Weekly</option><option value="on_demand">On Demand</option></select></div><Input label="Last Sync" type="datetime-local" value={data.last_sync_at ?? ''} onChange={(e) => setData('last_sync_at', e.target.value)} errors={errors.last_sync_at} /><Input label="Success Rate (%)" type="number" value={data.success_rate_pct} onChange={(e) => setData('success_rate_pct', e.target.value)} errors={errors.success_rate_pct} /><div><label className="text-gray-600 text-sm">PIC Integrasi</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.owner_id} onChange={(e) => setData('owner_id', e.target.value)}><option value="">- Pilih PIC -</option>{users.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><div><label className="text-gray-600 text-sm">Status</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.status} onChange={(e) => setData('status', e.target.value)}><option value="planned">Planned</option><option value="active">Active</option><option value="issue">Issue</option><option value="deprecated">Deprecated</option></select></div><div className="md:col-span-2"><label className="text-gray-600 text-sm">Catatan</label><textarea className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" rows={4} value={data.note ?? ''} onChange={(e) => setData('note', e.target.value)} /></div></div></Card></>;
}

Create.layout = (page) => <AppLayout children={page} />;
