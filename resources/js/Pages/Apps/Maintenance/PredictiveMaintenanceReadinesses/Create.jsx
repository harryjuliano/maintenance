import AppLayout from '@/Layouts/AppLayout';
import Card from '@/Components/Card';
import Input from '@/Components/Input';
import Button from '@/Components/Button';
import { Head, useForm } from '@inertiajs/react';
import { IconDeviceFloppy, IconCpu } from '@tabler/icons-react';

const defaultData = { readiness_no: '', assessment_date: '', asset_id: '', data_quality_score: 0, sensor_coverage_pct: 0, failure_model_status: 'not_started', readiness_level: 'low', notes: '', status: 'draft' };

export default function Create({ assets = [], initialData = defaultData, isEdit = false }) {
    const { data, setData, post, put, errors } = useForm({ ...defaultData, ...initialData, asset_id: initialData.asset_id ?? '' });
    const submit = (e) => { e.preventDefault(); isEdit ? put(route('apps.predictive-maintenance-readinesses.update', initialData.id)) : post(route('apps.predictive-maintenance-readinesses.store')); };

    return <><Head title={isEdit ? 'Ubah Predictive Maintenance Readiness' : 'Tambah Predictive Maintenance Readiness'} /><Card title={isEdit ? 'Ubah Predictive Maintenance Readiness' : 'Tambah Predictive Maintenance Readiness'} icon={<IconCpu size={18} strokeWidth={1.5} />} form={submit} footer={<Button type="submit" icon={<IconDeviceFloppy size={18} strokeWidth={1.5} />} label="Simpan" variant="gray" />}><div className="grid md:grid-cols-2 gap-4 mb-4"><Input label="No Readiness" type="text" value={data.readiness_no} onChange={(e) => setData('readiness_no', e.target.value)} errors={errors.readiness_no} /><Input label="Tanggal Assessment" type="date" value={data.assessment_date} onChange={(e) => setData('assessment_date', e.target.value)} errors={errors.assessment_date} /><div><label className="text-gray-600 text-sm">Asset</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.asset_id} onChange={(e) => setData('asset_id', e.target.value)}><option value="">- Pilih Asset -</option>{assets.map((item) => <option key={item.id} value={item.id}>{item.asset_name}</option>)}</select></div><Input label="Data Quality Score (%)" type="number" value={data.data_quality_score} onChange={(e) => setData('data_quality_score', e.target.value)} errors={errors.data_quality_score} /><Input label="Sensor Coverage (%)" type="number" value={data.sensor_coverage_pct} onChange={(e) => setData('sensor_coverage_pct', e.target.value)} errors={errors.sensor_coverage_pct} /><div><label className="text-gray-600 text-sm">Failure Model Status</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.failure_model_status} onChange={(e) => setData('failure_model_status', e.target.value)}><option value="not_started">Not Started</option><option value="training">Training</option><option value="validated">Validated</option><option value="deployed">Deployed</option></select></div><div><label className="text-gray-600 text-sm">Readiness Level</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.readiness_level} onChange={(e) => setData('readiness_level', e.target.value)}><option value="low">Low</option><option value="medium">Medium</option><option value="high">High</option><option value="best_in_class">Best in Class</option></select></div><div><label className="text-gray-600 text-sm">Status</label><select className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" value={data.status} onChange={(e) => setData('status', e.target.value)}><option value="draft">Draft</option><option value="reviewed">Reviewed</option><option value="approved">Approved</option></select></div><div className="md:col-span-2"><label className="text-gray-600 text-sm">Catatan</label><textarea className="w-full mt-2 px-3 py-2 rounded-md border bg-white dark:bg-gray-900 dark:border-gray-800" rows={4} value={data.notes ?? ''} onChange={(e) => setData('notes', e.target.value)} /></div></div></Card></>;
}

Create.layout = (page) => <AppLayout children={page} />;
